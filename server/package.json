{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure Lexigo game logic (grid, dictionary, validation, scoring, solver, bots) reused by the Cloudflare Worker. No longer a running server.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src tests"
  },
  "devDependencies": {
    "@eslint/js": "^9.10.0",
    "eslint": "^9.10.0",
    "vitest": "^2.1.0"
  }
}
